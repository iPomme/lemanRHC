# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# JSON sample
GET /graph.json                     controllers.Application.graph
GET /graphAccount.json                     controllers.Application.graphAccount
GET /graphCustomer.json                     controllers.Application.graphCustomer
GET /graphCurrency.json                     controllers.Application.graphCurrency
GET /bullets.json                     controllers.Application.bullets
GET /version.json                     controllers.Application.version